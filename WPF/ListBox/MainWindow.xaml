<Window x:Class="ListBox.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ListBox"
        mc:Ignorable="d"
        Title="ListBox" Width="300" Height="400" FontSize="16" ResizeMode="CanMinimize" >
    <Grid FocusManager.FocusedElement="{Binding ElementName=tbInput}">
		<Grid.RowDefinitions>
			<RowDefinition Height="40" />
			<RowDefinition />
		</Grid.RowDefinitions>
		<Grid.ColumnDefinitions>
			<ColumnDefinition />
			<ColumnDefinition />
		</Grid.ColumnDefinitions>
		
		<TextBox Name="tbInput" 
				 Margin="5" 
				 HorizontalContentAlignment="Center" VerticalContentAlignment="Center"
				 KeyDown="TbInput_KeyDown"
				 />
		
		<ListBox Name="listBox"
				 Grid.Row="1" Grid.ColumnSpan="2" 
				 Margin="5" 
				 />

		<Grid Grid.Row="0" Grid.Column="1">
			<Grid.ColumnDefinitions>
				<ColumnDefinition />
				<ColumnDefinition />
				<ColumnDefinition />
			</Grid.ColumnDefinitions>
			<Button Content="ADD" Grid.Column="0" Margin="5" Click="button_Click" Name="btnAdd" />
			<Button Content="DEL" Grid.Column="1" Margin="5" Click="button_Click" />
			<Button Content="CLR" Grid.Column="2" Margin="5" Click="button_Click" />
		</Grid>

	</Grid>
</Window>
